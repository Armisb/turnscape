For development purposes make sure unity is looking at localhost:7232

Build and run steps
1. run `dotnet build` in the shared_lib directory
2. run `dotnet run` in the server/GameAPI directory
3. Open Unity at the non-root turnscape directory
